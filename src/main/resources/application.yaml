server:
  port: 8090

spring:
  application:
    name: jangmin

  datasource:
    url: jdbc:mysql://localhost:3306/jang
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
